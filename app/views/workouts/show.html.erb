<div class="workout-box col-xs-10 col-xs-offset-1">
	<h2><%= @workout.workout_type_name %></h2>
	<h3><%= @workout.created_at.strftime "%B %e, %Y" %></h3>
	<br />

  <% @workout.exercises.each do |exercise| %>
  	<div class="pull-left exercise-name"><%= exercise.name %></div>
  	<table class="table table-bordered table-striped table-responsive">
		  <thead>
		    <tr>
		      <th><%= "Set" %></th>
		      <th><%= "Reps" %></th>
		      <th><%= "Load (lb)" %></th>
		    </tr>
		  </thead>
    	<% set_number = 1 %>
    	<tbody>
	    	<% exercise.completed_sets.each do |set| %>
		      <tr>
		        <td><%= set_number %></td>
		        <td><%= set.reps %></td>
		        <td><%= set.load %></td>
		      </tr>
		      <% set_number += 1 %>
	      <% end %>
	    </tbody>
    </table>
  <% end %>
</div>
<div class="clear"></div>

<div class="link col-xs-10 col-xs-offset-1">
	<%= link_to user_workouts_path(@workout.user) do %>
		<span class="glyphicon glyphicon-circle-arrow-left" aria-hidden="true"></span>
		Back to List of Your Workouts &nbsp;
	<% end %>
</div>